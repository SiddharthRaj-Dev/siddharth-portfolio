
import React from 'react';import MotionSection from '../components/MotionSection';export default function Certificates(){const items=[];return(<MotionSection id="certificates" className="mx-auto max-w-6xl px-4 py-20"><h2 className="section-title">Certificates</h2>{items.length===0?(<p className="text-slate-400 mt-2">No certificates yet. Share details and I’ll add them here.</p>):(<div className="mt-6 grid md:grid-cols-2 gap-6">{items.map(c=>(<div key={c.title} className="glass p-6 glow-edge"><h3 className="text-lg font-semibold">{c.title}</h3><p className="text-slate-300">{c.issuer} • {c.year}</p>{c.link&&<a href={c.link} className="ripple-effect btn btn-unified">View</a>}</div>))}</div>)}</MotionSection>)}