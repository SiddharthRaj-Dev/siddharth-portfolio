
import React from 'react';import MotionSection from '../components/MotionSection';export default function Projects(){const items=[{title:'Project One (Add your real project)',desc:'Short 1–2 line description about what it solves and your role.',stack:['Python','Pandas'],link:'#'},{title:'Project Two (Add your real project)',desc:'Short 1–2 line description about what it solves and your role.',stack:['HTML','CSS'],link:'#'}];return(<MotionSection id="projects" className="mx-auto max-w-6xl px-4 py-20"><h2 className="section-title">Projects</h2><div className="mt-6 grid md:grid-cols-2 gap-6">{items.map(p=>(<article key={p.title} className="glass p-6 glow-edge"><h3 className="text-lg font-semibold">{p.title}</h3><p className="text-slate-300 mt-2">{p.desc}</p><div className="mt-3 flex flex-wrap gap-2">{p.stack.map(t=><span key={t} className="badge">{t}</span>)}</div><div className="mt-4"><a href={p.link} className="ripple-effect btn btn-unified">View</a></div></article>))}</div></MotionSection>)}